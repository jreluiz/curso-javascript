# Aula 01 — Revisão: Múltipla Escolha

> 🎯 8 questões sobre a [Aula 01 — Introdução ao JavaScript](../README.md). Só uma alternativa está correta em cada uma.

**Sem gabarito, de propósito.** Cada questão termina com a seção da aula onde a resposta está. Responda **tudo primeiro**, sem consultar — só depois volte às seções indicadas e corrija. Errar e ter que caçar a resposta é o que faz o conceito grudar.

---

### Q-A01-01

Sobre a relação entre JavaScript e Java, é correto afirmar que:

- **a)** JavaScript é uma versão simplificada de Java, criada para rodar no navegador;
- **b)** JavaScript também é compilado para a JVM, como o Java;
- **c)** são linguagens completamente diferentes, e a semelhança nos nomes veio de uma decisão de marketing;
- **d)** Java tem tipagem dinâmica e JavaScript tem tipagem estática.

↩︎ *Aula 01, seção 1 — O que é JavaScript?*

---

### Q-A01-02

O que o Node.js acrescentou ao JavaScript a partir de 2009?

- **a)** A tradução do código JavaScript para Java antes da execução;
- **b)** um navegador próprio, oficial da linguagem;
- **c)** a compilação do JavaScript em um arquivo executável independente;
- **d)** a capacidade de executar JavaScript fora do navegador, como no terminal e em servidores.

↩︎ *Aula 01, seção 1 — O que é JavaScript?*

---

### Q-A01-03

Você criou o arquivo `ola.js` dentro da pasta `aula-01` e está nessa pasta pelo terminal. O comando que executa o programa é:

- **a)** `run ola.js`
- **b)** `node ola.js`
- **c)** `javascript ola.js`
- **d)** `console.log ola.js`

↩︎ *Aula 01, seção 3 — Primeiro programa*

---

### Q-A01-04

O que este programa imprime no terminal?

```javascript
console.log("Linha 1");
// console.log("Linha 2");
console.log(2 + 3);
```

- **a)** `Linha 1`, `Linha 2` e `5`, em três linhas;
- **b)** `Linha 1` e `Linha 2`, em duas linhas;
- **c)** `Linha 1` e `5`, em duas linhas;
- **d)** `Linha 1`, `Linha 2` e `2 + 3`, em três linhas.

↩︎ *Aula 01, seção 4 — Vários `console.log` e comentários*

---

### Q-A01-05

O que aparece no terminal ao executar `console.log("2" + "3");`?

- **a)** `23`
- **b)** `5`
- **c)** `"2" + "3"`
- **d)** `NaN`

↩︎ *Aula 01, seção 4 — Vários `console.log` e comentários*

---

### Q-A01-06

Ao executar um arquivo, o Node exibe a mensagem `SyntaxError: missing ) after argument list`. Isso indica que:

- **a)** uma variável foi usada antes de ser declarada;
- **b)** o arquivo informado não foi encontrado;
- **c)** o resultado de uma conta não é um número válido;
- **d)** existe um parêntese que não foi fechado no código.

↩︎ *Aula 01, seção 5 — Erros são normais (e informativos)*

---

### Q-A01-07

O curso começa pelo Node.js, e não pelo navegador, principalmente porque:

- **a)** permite focar na lógica, sem a distração de HTML e CSS, com o fluxo escrever arquivo → executar → ver a saída no terminal;
- **b)** os navegadores só entendem versões antigas de JavaScript;
- **c)** a função `console.log` não existe no navegador;
- **d)** programas em Node executam mais rápido que os do navegador.

↩︎ *Aula 01, seção 2 — Por que começamos no Node.js*

---

### Q-A01-08

Sobre o `;` no fim das instruções em JavaScript:

- **a)** é obrigatório: sem ele o programa falha com `SyntaxError`;
- **b)** é tecnicamente opcional na linguagem, mas o curso adota o uso sempre;
- **c)** só é necessário dentro de funções e blocos;
- **d)** serve para transformar o restante da linha em comentário.

↩︎ *Aula 01, seção 3 — Entendendo a linha*

---

⬅️ [Voltar à Aula 01](../README.md) | ➡️ [Revisão da Aula 02](../../aula-02-variaveis-tipos/revisao/README.md)
