# Aula 09 — Revisão: Múltipla Escolha

> 🎯 8 questões sobre a [Aula 09 — HTML + CSS + JavaScript no Navegador](../README.md). Só uma alternativa está correta em cada uma.

**Sem gabarito, de propósito.** Cada questão termina com a seção da aula onde a resposta está. Responda **tudo primeiro**, sem consultar — só depois volte às seções indicadas e corrija.

---

### Q-A09-01

No trio da web, o papel de cada linguagem é:

- **a)** HTML define a aparência, CSS define a estrutura e JavaScript define o conteúdo;
- **b)** HTML define a estrutura e o conteúdo, CSS define a aparência e JavaScript define o comportamento;
- **c)** HTML e CSS são linguagens de programação, e JavaScript é uma linguagem de marcação;
- **d)** as três fazem a mesma coisa, mudando apenas a sintaxe.

↩︎ *Aula 09, seção 1 — O trio da web*

---

### Q-A09-02

A diferença entre os atributos `id` e `class` é que:

- **a)** `id` só pode ser usado pelo CSS e `class` só pelo JavaScript;
- **b)** `class` identifica um elemento de forma única, enquanto `id` pode se repetir;
- **c)** `id` identifica um elemento de forma única, enquanto `class` é um rótulo reutilizável por vários elementos;
- **d)** não há diferença: são dois nomes para o mesmo atributo.

↩︎ *Aula 09, seção 2 — HTML essencial*

---

### Q-A09-03

Para estilizar no CSS o elemento `<div id="area-especial">`, o seletor correto é:

- **a)** `#area-especial`
- **b)** `.area-especial`
- **c)** `area-especial`
- **d)** `div.area-especial`

↩︎ *Aula 09, seção 3 — CSS mínimo*

---

### Q-A09-04

A tag `<script src="script.js"></script>` é colocada, por convenção, no **final do `<body>`** porque:

- **a)** o navegador ignora qualquer script que esteja no `<head>`;
- **b)** essa é a única posição que o navegador aceita para a tag `<script>`;
- **c)** isso faz o CSS da página carregar mais rápido;
- **d)** assim o HTML já foi carregado quando o script executa, e os elementos existem para o JavaScript encontrar.

↩︎ *Aula 09, seção 4 — JavaScript entra em cena*

---

### Q-A09-05

Um `script.js` ligado a uma página contém apenas `console.log("Rodando!");`. Ao abrir a página no navegador, a mensagem `Rodando!` aparece:

- **a)** no corpo da página, como texto;
- **b)** no Console do DevTools;
- **c)** numa janela pop-up sobre a página;
- **d)** no título da aba do navegador.

↩︎ *Aula 09, seção 5 — DevTools*

---

### Q-A09-06

Segundo a regra do Bloco 3, ao encontrar um problema numa página, o primeiro passo é:

- **a)** abrir o Console do DevTools e ler o erro, que traz o arquivo e a linha;
- **b)** reescrever o HTML do zero;
- **c)** testar a página em outro navegador;
- **d)** apagar o CSS para isolar o problema.

↩︎ *Aula 09, seção 5 — DevTools*

---

### Q-A09-07

Sobre as tags HTML, é correto afirmar que:

- **a)** toda tag precisa obrigatoriamente de uma tag de fechamento;
- **b)** tags não podem receber atributos;
- **c)** a tag `<li>` funciona sozinha, fora de uma lista;
- **d)** a maioria abre e fecha (`<p>...</p>`), mas algumas não têm tag de fechamento, como `<input />`.

↩︎ *Aula 09, seção 2 — HTML essencial*

---

### Q-A09-08

A diferença entre `alert("oi")` e `console.log("oi")` é que:

- **a)** são equivalentes: os dois abrem uma janela;
- **b)** `alert` só funciona no Node e `console.log` só no navegador;
- **c)** `alert` interrompe a página com uma janela visível ao usuário, enquanto `console.log` escreve no Console, para quem desenvolve;
- **d)** `console.log` escreve a mensagem na página e `alert` escreve no Console.

↩︎ *Aula 09, seção 4 — JavaScript entra em cena*

---

⬅️ [Voltar à Aula 09](../README.md) | ➡️ [Revisão da Aula 10](../../aula-10-dom/revisao/README.md)
